rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // The Admin SDK uploads files; public images use generated download-token URLs.
    match /{path=**} { allow read, write: if false; }
  }
}
