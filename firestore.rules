rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access passes through the Express API and its per-feature permissions.
    match /{document=**} { allow read, write: if false; }
  }
}
